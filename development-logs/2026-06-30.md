# 开发日志 — 2026-06-30

## 今日完成

### Phase 12：AI 建议持久化 + 代理模式优化

- [x] **移除火山方舟 API**：删除 Ark 供应商所有代码，简化为单一 DeepSeek 架构
  - `js/api.js`：`AI_PROVIDERS` → `AI_PROVIDER`，删除 `getActiveProvider/setActiveProvider/getProvider`，简化所有函数签名
  - `js/settings.js`：删除 `switchProviderUI()`、`updateApiStatusBadgeForProvider()`
  - `index.html`：删除供应商选择器，改为静态标签
  - `proxy/` 全部配置文件删除 Ark 路由

- [x] **AI 建议持久化**：退出后重进不丢失
  - `js/api.js`：新增 `saveAIAdvice(dateStr, data)` / `loadAIAdvice(dateStr)`，存储键 `cc_ai_advice_YYYY-MM-DD`
  - `js/advisor.js`：`renderAdvice()` 自动恢复已保存建议；`requestAIAdvice()` 成功后自动保存
  - 按钮文字根据是否有缓存显示「🤖 AI 深度分析」/「🤖 重新分析」

- [x] **默认 API Key**：用户零配置使用 AI
  - 密钥仅存储于服务端：`functions/api/deepseek/chat/completions.js`
  - 前端代码零泄露（搜索 `sk-54d6d4` 结果为 0）
  - 代理配置文件填入默认密钥：nginx.conf / cloudflare-worker.js / vercel.json
  - 设置 UI 代理模式 + 无用户 Key → 徽章显示「默认密钥」

- [x] **安全加固**：
  - `functions/proxy/[[path]].js`：拦截所有 `/proxy/*` 请求返回 404，防止配置文件泄露
  - Cloudflare Pages Functions 优先于静态文件，密钥文件无法被公开访问

### Phase 13：AI 每日食谱计划 + 底部导航重构

- [x] **AI 每日食谱功能**：
  - `js/api.js`：新增 `getDailyMealPlan(userProfile, dailyTarget)` — AI 生成全天饮食+训练计划
  - `index.html`：新增食谱面板（空状态 + 生成按钮 + 结果展示区）
  - `js/advisor.js`：新增 `renderMealPlan()` / `generateMealPlan()` / `renderMealPlanResultHTML()`
  - 食谱包含：早餐/午餐/晚餐/加餐 + 训练计划 + 每日小贴士
  - 持久化存储：`cc_mealplan_YYYY-MM-DD`，按日期隔离

- [x] **底部导航重构**：2Tab → 3Tab
  - 新增「🍽️ AI食谱」独立 Tab
  - `js/app.js`：`switchTab()` 新增 `mealplan` 分支；新增 `showMealPlanView()`
  - 视图隔离：每个 Tab 只显示自己的内容，根除食谱重复出现的问题

- [x] **返回按钮**：
  - `index.html`：顶部导航新增 `← 返回` 按钮（默认隐藏）
  - `js/app.js`：子视图（历史/AI食谱）显示返回按钮、隐藏日期导航
  - `css/style.css`：`.nav-btn--back` 绿色加粗样式

- [x] **设置弹窗手机端适配**：
  - 新增 `modal-header`（sticky 固定标题栏 + `✕` 关闭按钮）
  - 新增 `modal-body` 滚动容器（`max-height: 90vh` + `overflow-y: auto`）
  - 解决小屏手机设置内容显示不全的问题

## 文档同步

- [x] `docs/01-project-requirements.md`：新增 AI 食谱计划、代理模式、3Tab 导航
- [x] `docs/02-technical-notes.md`：更新项目结构、存储方案、AI 集成文档
- [x] `docs/04-development-plan.md`：新增 Phase 12/13，更新进度表
- [x] `development-logs/2026-06-30.md`：创建今日日志

## 部署

- [x] 同步到 GitHub：https://github.com/Tout-est-parfait/calorie-calculator
- [x] Cloudflare Pages 自动部署（含 Functions API 代理）

## 修改文件汇总

| 文件 | 改动说明 |
|------|----------|
| `index.html` | 删除供应商选择器；新增食谱面板；底部导航+1Tab；返回按钮；modal-header/body |
| `js/api.js` | 移除 Ark；新增 save/loadAIAdvice；新增 getDailyMealPlan/saveMealPlan/loadMealPlan |
| `js/settings.js` | 移除供应商切换；状态徽章新增「默认密钥」；绑定关闭按钮 |
| `js/advisor.js` | AI建议持久化恢复+保存；新增 renderMealPlan/generateMealPlan 等 |
| `js/app.js` | 3Tab导航；新增 showMealPlanView；返回按钮显隐；refreshView 条件刷新 |
| `css/style.css` | 食谱卡片样式；返回按钮；modal 滚动适配 |
| `functions/api/deepseek/chat/completions.js` | 新建 — Cloudflare Pages API 代理 |
| `functions/proxy/[[path]].js` | 新建 — 拦截 /proxy/* 访问 |
| `proxy/nginx.conf` | 删除 Ark；填入默认密钥 |
| `proxy/cloudflare-worker.js` | 删除 Ark 路由；填入默认密钥 |
| `proxy/vercel.json` | 删除 Ark rewrite；填入默认密钥 |
| `proxy/README.md` | 删除 Ark 说明 |
| `docs/01/02/04` | 同步更新 |
