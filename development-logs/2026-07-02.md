# 开发日志 — 2026-07-02

## 今日完成

### Phase 17：弹窗 UI 重构 + AI 每日食谱智能优化

- [x] **自定义食物弹窗重构**：
  - `index.html`：新增 `.modal-header`（标题 + 关闭按钮 ✕）和 `.modal-body` 包裹，统一与其他弹窗的 padding 规范
  - `index.html`：碳水/蛋白质/脂肪标签统一加"每 100g"前缀，消除计量单位歧义
  - `index.html`：移除 `.form-row-3` 三列窄布局，每个营养素改为独立行（`form-field`），避免窄列中单位文字与输入框视觉重叠
  - `css/style.css`：`.modal-desc` 改为 `margin-bottom` 代替 `margin-top: -12px`，增加与表单的间距
  - `css/style.css`：`.custom-food-form` gap 从 `var(--space-md)` 加大到 `var(--space-lg)`
  - `js/app.js`：绑定新增的 `btn-cf-close` 关闭按钮事件

- [x] **AI 智能估计弹窗重构**：
  - `index.html`：新增 `.modal-header`（标题 + 关闭按钮 ✕）和 `.modal-body` 包裹
  - `index.html`：描述文字拆分为主描述 + 独立提示行（`modal-desc-hint`），增强层次感和呼吸感
  - `index.html`：textarea 从 3 行增加到 4 行
  - `css/style.css`：新增 `.modal-desc-hint` 样式（小字灰色提示）
  - `css/style.css`：`.ai-estimate-form` gap 增大 + 底部 margin，与按钮区分开
  - `css/style.css`：`.ai-estimate-char-count` 增加 `margin-top`
  - `js/app.js`：绑定新增的 `btn-ai-close` 关闭按钮事件

- [x] **AI 每日食谱功能优化**：
  - `index.html`：新增忌口偏好输入框（`mealplan-restrictions`），选填
  - `index.html`：生成按钮移出空状态区，始终可见（标题 → 忌口 → 按钮 → 内容区）
  - `index.html`：空状态描述更新，说明 AI 会根据身体数据+摄入+时间智能生成
  - `css/style.css`：新增 `.mealplan-prefs` / `.mealplan-prefs-input` 样式
  - `css/style.css`：`.mealplan-section` 改为 flex 布局，gap 统一间距
  - `js/advisor.js`：新增 `getRemainingMealsContext()` 函数，根据当前小时自动判断剩余餐次：
    - 0-10点 → 全日（早+中+晚+加餐）
    - 10-14点 → 剩余（午+晚+加餐）
    - 14-17点 → 剩余（晚+加餐）
    - 17点后 → 轻加餐/明日早餐建议
  - `js/advisor.js`：`renderMealPlan()` 重写 — 按钮始终绑定，文字动态切换（"生成今日食谱" / "重新生成食谱"）
  - `js/advisor.js`：`generateMealPlan()` 重写 — 读取忌口偏好、计算剩余餐次、传入已摄入营养素（碳水/蛋白/脂肪）详情，失败时 toast 提示而非替换空状态
  - `js/api.js`：`getDailyMealPlan()` 新增 `dietaryRestrictions` 和 `remainingMeals` 参数
  - `js/api.js`：system prompt 增强 — 要求遵守忌口、识别已用过的餐次、剩余餐次热量 = 目标 - 已摄入

## 修改文件汇总

| 文件 | 改动说明 |
|------|----------|
| `index.html` | 自定义食物弹窗重构（header+body、标签"每100g"、三列→独立行）；AI估计弹窗重构（header+body、描述拆分、行数增加）；AI食谱区增加忌口输入、按钮外置 |
| `css/style.css` | `.modal-desc` 间距调整；新增 `.modal-desc-hint`；`.custom-food-form` gap 加大；`.ai-estimate-form` 间距优化；新增 `.mealplan-prefs` 系列样式；`.mealplan-section` flex 布局 |
| `js/app.js` | 绑定 `btn-cf-close` 和 `btn-ai-close` 关闭按钮事件 |
| `js/advisor.js` | 新增 `getRemainingMealsContext()`；重写 `renderMealPlan()`（按钮常驻）；重写 `generateMealPlan()`（忌口+智能餐次+增强上下文） |
| `js/api.js` | `getDailyMealPlan()` 新增 `dietaryRestrictions` + `remainingMeals` 参数；prompt 强化（忌口遵守、已用餐次识别、剩余预算计算） |
